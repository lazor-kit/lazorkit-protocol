/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita 
 */

import * as beet from '@metaplex-foundation/beet';
import * as web3 from '@solana/web3.js';
import * as beetSolana from '@metaplex-foundation/beet-solana';




/**
 * Arguments used to create {@link FeeRecordAccount}
 * @category Accounts
 * @category generated
 */
export type FeeRecordAccountArgs = {
  discriminator: number
  bump: number
  version: number
  padding: number[] /* size: 5 */
  totalFeesPaid: beet.bignum
  txCount: number
  walletCount: number
  registeredAt: beet.bignum
}

;
/**
 * Holds the data for the {@link FeeRecordAccount} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class FeeRecordAccount implements FeeRecordAccountArgs {
  private constructor(
    readonly discriminator: number,
    readonly bump: number,
    readonly version: number,
    readonly padding: number[] /* size: 5 */,
    readonly totalFeesPaid: beet.bignum,
    readonly txCount: number,
    readonly walletCount: number,
    readonly registeredAt: beet.bignum
  ) {}

  /**
   * Creates a {@link FeeRecordAccount} instance from the provided args.
   */
  static fromArgs(args: FeeRecordAccountArgs) {
    return new FeeRecordAccount(
      args.discriminator,
      args.bump,
      args.version,
      args.padding,
      args.totalFeesPaid,
      args.txCount,
      args.walletCount,
      args.registeredAt
    );
  }

  /**
   * Deserializes the {@link FeeRecordAccount} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [ FeeRecordAccount, number ]  {
    return FeeRecordAccount.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link FeeRecordAccount} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig,
  ): Promise<FeeRecordAccount> {
    const accountInfo = await connection.getAccountInfo(address, commitmentOrConfig);
    if (accountInfo == null) {
      throw new Error(`Unable to find FeeRecordAccount account at ${address}`);
    }
    return FeeRecordAccount.fromAccountInfo(accountInfo, 0)[0];
  }


  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId: web3.PublicKey = new web3.PublicKey('FLb7fyAtkfA4TSa2uYcAT8QKHd2pkoMHgmqfnXFXo7ao')) {
    return beetSolana.GpaBuilder.fromStruct(programId, feeRecordAccountBeet)
  }

  /**
   * Deserializes the {@link FeeRecordAccount} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(
    buf: Buffer,
    offset = 0
  ): [ FeeRecordAccount, number ]{
    return feeRecordAccountBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link FeeRecordAccount} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [ Buffer, number ] {
    return feeRecordAccountBeet.serialize(this)
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link FeeRecordAccount}
   */
  static get byteSize() {
    return feeRecordAccountBeet.byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding 
   * {@link FeeRecordAccount} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      FeeRecordAccount.byteSize,
      commitment,
    );
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link FeeRecordAccount} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === FeeRecordAccount.byteSize;
  }

  /**
   * Returns a readable version of {@link FeeRecordAccount} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      discriminator: this.discriminator,
      bump: this.bump,
      version: this.version,
      padding: this.padding,
      totalFeesPaid: (() => {
        const x = <{ toNumber: () => number }>this.totalFeesPaid
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })(),
      txCount: this.txCount,
      walletCount: this.walletCount,
      registeredAt: (() => {
        const x = <{ toNumber: () => number }>this.registeredAt
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })()
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const feeRecordAccountBeet = new beet.BeetStruct<
    FeeRecordAccount,
    FeeRecordAccountArgs
>(
  [
    
    ['discriminator', beet.u8],
    ['bump', beet.u8],
    ['version', beet.u8],
    ['padding', beet.uniformFixedSizeArray(beet.u8, 5)],
    ['totalFeesPaid', beet.u64],
    ['txCount', beet.u32],
    ['walletCount', beet.u32],
    ['registeredAt', beet.u64]
  ],
  FeeRecordAccount.fromArgs,
  'FeeRecordAccount'
)